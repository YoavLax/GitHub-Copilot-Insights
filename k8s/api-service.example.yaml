apiVersion: v1
kind: Service
metadata:
  name: copilot-insights-api
  namespace: default  # Change to your namespace
  labels:
    app: copilot-insights-api
spec:
  type: ClusterIP
  ports:
  - port: 3001
    targetPort: 3001
    protocol: TCP
    name: http
  selector:
    app: copilot-insights-api
