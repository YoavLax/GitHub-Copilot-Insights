apiVersion: v1
kind: Service
metadata:
  name: copilot-insights
  namespace: default  # Change to your namespace
  labels:
    app: copilot-insights
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: copilot-insights
